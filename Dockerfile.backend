FROM node:22-alpine

WORKDIR /app

COPY backend/package.json ./package.json
COPY backend/src ./src
COPY backend/scripts ./scripts

ENV NODE_ENV=production
ENV NAERO_SERVICE_ENV=production
ENV PORT=8787

EXPOSE 8787

CMD ["node", "src/server.js"]
